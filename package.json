{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "serve": "concurrently \"npm run dev\" \"php artisan serve --host=0.0.0.0 --port=8000\"",
        "vercel-build": "npm run build && cp -r public public/build"
    },
    "dependencies": {
        "@vercel/php": "^1.15.0",
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^1.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^5.4.0"
    }
}
